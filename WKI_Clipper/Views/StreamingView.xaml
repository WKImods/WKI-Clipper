<UserControl x:Class="WKI_Clipper.Views.StreamingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Connection card (built in code-behind) -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" CornerRadius="6"
                Padding="12,10" Margin="0,0,0,10">
            <StackPanel x:Name="ConnectionPanel" />
        </Border>

        <!-- Stream-deck grid -->
        <UniformGrid x:Name="DeckGrid" Grid.Row="1" />

        <!-- Status line -->
        <TextBlock x:Name="StatusLine" Grid.Row="2" Margin="2,8,2,0"
                   Style="{StaticResource MutedStyle}" TextTrimming="CharacterEllipsis" />
    </Grid>
</UserControl>
