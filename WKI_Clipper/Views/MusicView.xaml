<UserControl x:Class="WKI_Clipper.Views.MusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Now playing + transport -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" CornerRadius="6"
                Padding="12,10" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ArtistText" Style="{StaticResource MutedStyle}"
                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                <StackPanel x:Name="Transport" Orientation="Horizontal" Margin="0,8,0,0" />
            </StackPanel>
        </Border>

        <!-- Levels (stream + monitor) -->
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="6"
                Padding="12,10" Margin="0,0,0,8">
            <StackPanel x:Name="LevelsPanel" />
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="TrackList" />
        </ScrollViewer>

        <StackPanel x:Name="FooterPanel" Grid.Row="3" Margin="0,8,0,0" />
    </Grid>
</UserControl>
